import { Box } from "@mui/material";
import { styled } from "styled-components";

export const NotFoundStyle = styled(Box)`
  width: 100%;
  min-height: 400px;
  display: flex;
  justify-content: center;
  align-items: center;

  .not_found {
    font-size: 24px;
  }
`;
